name: web-brave
version: 1.0.0
manifest_version: 1
description: "Brave Search — Flowly's default web search backend (direct Brave API key, or the Flowly Cloud search proxy when logged in)."
author: "Flowly"
kind: backend
provides_web_providers:
  - brave
